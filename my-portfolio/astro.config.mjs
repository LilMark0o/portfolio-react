import { defineConfig } from 'astro/config';

export default defineConfig({
  base: '/portfolio-react/', // Your GitHub repository name
  site: 'https://lilmark0o.github.io', // Your GitHub Pages site
});
