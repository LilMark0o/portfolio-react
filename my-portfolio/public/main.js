// Example: Initialize your typing animation or other JS functionality here.
document.addEventListener('DOMContentLoaded', () => {
  const typedElement = document.getElementById('typed');
  if (typedElement) {
    typedElement.innerText = 'Welcome to my portfolio!';
    // You could integrate a library like Typed.js here if desired.
  }
});
